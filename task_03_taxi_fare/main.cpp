
#include <cmath>
#include <iomanip>
#include <iostream>
using namespace std;
int main() {
    long long k;
    if (!(std::cin >> k))
        return 0;
    // TODO: compute the fare as specified and print with two decimals
    return 0;
}


#include <iostream>
using namespace std;
int main() {

    unsigned long long n;
    if (!(cin >> n))
        return 0;
    long long reversed = 0;

    // TODO: reverse using % and /, not strings
    return 0;
}
